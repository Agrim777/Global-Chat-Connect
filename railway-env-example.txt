Copy these variable names into your deployment environment.

TELEGRAM_BOT_TOKEN       → Telegram bot token from @BotFather
ADMIN_TELEGRAM_ID        → numeric Telegram ID that receives safety/admin alerts
DATABASE_URL             → PostgreSQL connection string
SESSION_SECRET           → deployment secret if your platform requires it
BOT_POLLING_ENABLED      → true
NODE_ENV                 → production
